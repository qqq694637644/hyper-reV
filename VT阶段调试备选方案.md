# VT阶段调试备选方案

## 备选方案（暂存）
- 方案3：`极简事件码 + TX-only 网卡单向发包`。
- 目标：在 VT 启动窗口中，即使串口吞吐不足，也能尽量把关键阶段信息实时发到宿主侧采集器。
- 约束：仅做发送（不做接收/中断），固定小结构体（如 stage_id、rip、cr3、cpu、tsc），避免复杂逻辑导致时序抖动。

## 当前状态
- 已标记为“备选方案”，后续可按需要切换实现。
